class Rabbit
{
  field Board board;

  constructor Rabbit new(Board board_in)
  {
    let board = board_in;
    return this;
  }

  method void move()
  {
    var int dir;
    let dir = Random.next(8);
    do board.moveRabbit(Random.next(8));
    return;
  }
}